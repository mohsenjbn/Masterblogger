@page
@model BL.Presentation.Areas.Admin.Pages.ListCategory.IndexModel
@{
}


<section class="ftco-section">
    <div class="container">
        <div class="row justify-content-center">
        </div>
        <div class="row">
            <div class="col-md-12">
                <div class="table-wrap">
                    <table class="table table-responsive-xl">
                        <thead>
                            <tr>

                                <th>#</th>
                                <th>Title</th>
                                <th>Status</th>

                                <th class="align-content-center">
                                    <a asp-area="Admin" asp-page="/ListCategory/Add">
                                        <button class=" btn-success col-12 ">Add</button>

                                    </a>
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.Categories)
                            {
                                <tr class="alert" role="alert">
                                    <td>@item.Id </td>

                                    <td class="d-flex align-items-center">
                                        <div class="pl-3 email">
                                            <span>@item.Title</span>
                                            <span>@item.CreationDate</span>
                                        </div>
                                    </td>
                                    @if (item.IsRemove == false)
                                    {
                                        <td class="status"><span class="active">Active</span></td>
                                    }
                                    else
                                    {
                                        <td class="status"><span class="waiting">Inactive</span></td>
                                    }
                                    <td>
                                        <div class="float-right">
                                            @if (item.IsRemove == false)
                                            {

                                               <a asp-page-handler="Delete" asp-route-Id="@item.Id" >
                                                   <span><i class="fas fa-trash-alt " style="color:red"></i></span>

                                               </a>

                                                  
                                                
                                            }
                                            else
                                            {
                                                <a  asp-page-handler="Activate" asp-route-Id="@item.Id">
                                                    <span><i class="fas fa-undo" style="color:green"></i></span>
                                                </a>

                                            }




                                        </div>
                                        <div>
                                            <a asp-page="./Edit" asp-route-id="@item.Id">
                                                <span><i class="far fa-edit"></i></span>
                                            </a>
                                        </div>


                                    </td>


                                    

                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</section>